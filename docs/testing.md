# Testing Guide

This guide outlines the test organization and strategy.

## Running Tests

Verify correctness:
- Unit tests: `pytest gesture_controller/tests/unit`
- Integration tests: `pytest gesture_controller/tests/integration`
- Benchmark tests: `pytest gesture_controller/tests/benchmarks`
- Replay tests: `pytest gesture_controller/tests/replay`
